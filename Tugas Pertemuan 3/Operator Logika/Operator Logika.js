function cekLogika(a, b) {
    console.log("a && b : " + (a && b)); // AND
    console.log("a || b : " + (a || b)); // OR
    console.log("!a : " + (!a));         // NOT
}

cekLogika(true, false);